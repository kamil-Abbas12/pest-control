export const siteConfig = {
  brand: "Pest Control",
  brandWordmark: "PESTCONTROL", // used in the navbar/footer lockup
  domain: "pestcontrol.topdoglead.com",
  phoneDisplay: "(954) 231-0151",
  phoneHref: "tel:+19542310151", // corrected — the pasted markup was missing a digit
  // Nationwide referral service — no single city/state to anchor copy to
  locationLabel: "Nationwide",
};

// Distinct from the reference site's teal-green + navy pairing.
// Blue reads equally trustworthy for a pest-control lead service,
// and lines up with the navy already used in the Navbar's dark button.
export const theme = {
  accent: "#2F6FED", // primary CTA blue
  accentHover: "#1D4ED8",
  accentTint: "#EEF3FF", // light callout backgrounds
  ink: "#0B1630", // deep navy — matches the Navbar's dark button
  ink2: "#122148", // slightly lighter navy, used for icon circles
  border: "#E5E7EB",
  iconBg: "#F1F4FA",
  star: "#FBBF24",
};